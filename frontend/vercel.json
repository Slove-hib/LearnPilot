{
  "_comment": "部署前将 YOUR_RENDER_URL 替换为 Render 后端实际地址，如 https://learnpilot.onrender.com",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://YOUR_RENDER_URL/api/:path*"
    }
  ]
}
